diff --git a/recipes/onnxruntime/all/conanfile.py b/recipes/onnxruntime/all/conanfile.py
index 6c3ac07..abc1234 100644
--- a/recipes/onnxruntime/all/conanfile.py
+++ b/recipes/onnxruntime/all/conanfile.py
@@ -32,6 +32,11 @@ class OnnxRuntimeConan(ConanFile):
         "shared": [True, False],
         "fPIC": [True, False],
         "with_xnnpack": [True, False],
         "with_cuda": [True, False],
+        # Prevent libcurl dependency (fails to build on Windows runners)
+        # NOTE: This option name is specific to date/3.0.1.
+        # Newer recipe versions use tz_db=system instead.
+        "date/*:use_system_tz_db": True,
     }
     default_options = {
         "shared": False,
@@ -65,7 +70,7 @@ class OnnxRuntimeConan(ConanFile):
     def _compilers_minimum_version(self):
         return {
             "Visual Studio": "17",
-            "msvc": "193",
+            "msvc": "192",
             "gcc": "9",
             "clang": "5",
             "apple-clang": "10",
